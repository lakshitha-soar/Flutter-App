import 'package:soar_app/models/question_types/question_type.dart';

class QNumber extends QuestionType {
  QNumber({defaultValue, responseValue})
      : super(defaultValue: defaultValue, responseValue: responseValue);
}
